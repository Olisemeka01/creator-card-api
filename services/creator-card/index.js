const createCreatorCard = require('./create');
const getCreatorCardBySlug = require('./get-by-slug');
const deleteCreatorCard = require('./delete');

module.exports = {
  createCreatorCard,
  getCreatorCardBySlug,
  deleteCreatorCard,
};
